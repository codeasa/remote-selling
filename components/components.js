import React from 'react';
import { styled } from '@material-ui/core/styles';
import Button from '@material-ui/core/Button';

export const FwdButton = styled(Button)({
    background: '#E87722',
    color: 'white',
    border: 0,
    borderRadius: 3,
    padding: '10px 20px',
    margin: '5px'
})
